#include <iostream>  // #include is an include command, <iostream> means input and output stream
using namespace std; // Indicate the following program uses std
// The main() function is called the main function, and a C program always executes from the main() function.
int main() {
    cout <<"Comments"; // cout is used to display information on a computer screen
    return 0; // Used to end the function currently executing and return control to the function that called this function
} // Indicates the scope of the variable