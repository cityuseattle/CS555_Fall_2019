#include <iostream> //In order to read or write to the standard input/output streams you need to include it.
using namespace std; //What that does is tell the compiler that symbol names defined in the std namespace are to be brought into your program's scope.

//Int main() is the main function from which the program executes.
int main() {
    cout << "Comments"; //Cout << "Comments"; The message "Comments" will be displayed on the screen.
    return 0; //Return 0; Terminates the main() function and returns a value of 0 to the calling process.
}