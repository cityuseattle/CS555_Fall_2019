#include <iostream> //header to import build-in library
using namespace std; //telling compiler to use standard naming, provides scope for identifier

//main function of the program
int main()
{
    cout << "Comments"; //print the word "Comments" on screen
    return 0; //act as an exit status, most functions are expected to return something, in this case, return 0 act as a confirmation that the program runs fine
} //the contents of the main() function should be written in the {}
