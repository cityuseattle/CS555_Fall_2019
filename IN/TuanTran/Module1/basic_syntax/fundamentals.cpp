#include <iostream>         // Import iostream library so that we use cin cout
using namespace std;        // This mean we do not have to write 'std' in our code

int main(){                 // a special function in C++, this function will be called when the program is executed
    cout << "Comments";     // Print out the word "Comments"
    return 0;               // Return 0
}                           // Closing braces of the main function