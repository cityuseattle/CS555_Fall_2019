#include <iostream> 
/* It is a C++ language header.
For this program, the header <iostream> is needed */ 
using namespace std;    // tells the compiler to use the std namespace.

int main() {            
/* is the main function where 
program execution begins. */
    cout << "Comments"; 
/* causes the message "Comments" to be 
displayed on the screen. */
    
    return 0;
/* terminates main( )function and causes it 
to return the value 0 to the calling process. */
}